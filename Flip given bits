from os import *
from sys import *
from collections import *
from math import *

def flipSomeBits(num, arr, n):
	# Write your code here.
	pass
def flipSomeBits(num, arr, n):

    for i in range(n):

        num ^= (1 << (arr[i] - 1))

    return num
