def kthSpecialNumber(l: int, r: int, k: int) -> int:
    # write your code here
    cccc = 0
    for i in range(l,r+1):
        if("101" in bin(i)[2:]):
            cccc+=1
            if(cccc==k):
                return i   
    return -1
