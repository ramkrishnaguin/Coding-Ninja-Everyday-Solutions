#include <bits/stdc++.h> 
#include <vector>

vector < int > termsOfAP(int x) {
    vector<int>ans(x);
    int num = 5;
    for(int i=1;i<=x;){ 
        if(num%4)ans[i-1] = num,i++;
        num+=3;
    }
    return ans;
}
